
const SectionIcon = ({ path }) => (
  <svg width="16" height="16" viewBox="0 0 256 256" fill="currentColor">
    <g color="rgb(104, 204, 88)">
      {path}
    </g>
  </svg>
);

export default SectionIcon;
